{
  "regions": ["iad1", "lhr1", "hnd1"],
  "buildCommand": "turbo build --filter=@caltext/api",
  "outputDirectory": "apps/api/.output",
  "functions": {
    "**": {
      "runtime": "bun@1.x"
    }
  }
}
